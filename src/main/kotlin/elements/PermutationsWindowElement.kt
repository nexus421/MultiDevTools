package elements

import androidx.compose.runtime.Composable

class PermutationsWindowElement : WindowElement() {
    override val name = "Permutations"

    @Composable
    override fun windowComposable() {
        TODO("Copy from BruteForceWindowElement. Without the BruteForce callback an print all results to the right side!")
    }
}